{
  "name": "gh-workflows",
  "private": true,
  "description": "Dev tooling pins; also exercises the reusable workflow's npm path (setup-node, npm ci, repo-local bins) in this repo's own CI.",
  "devDependencies": {
    "@secretlint/secretlint-rule-preset-recommend": "13.0.4",
    "markdownlint-cli2": "0.23.2",
    "prettier": "3.9.6",
    "secretlint": "13.0.4"
  }
}
